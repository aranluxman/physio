{
  "name": "physio-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Personal physiotherapy tracking web app (hip impingement rehab regimen).",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "typecheck": "tsc --noEmit",
    "preview": "npm run build && npx wrangler pages dev out",
    "deploy": "npm run build && npx wrangler pages deploy out",
    "test": "tsc src/lib/schedule.ts src/lib/date.ts src/lib/types.ts --outDir .test-build --module commonjs --target es2020 --moduleResolution node --strict && node tests/schedule.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "next": "14.2.15",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.13",
    "typescript": "^5.6.3"
  }
}
